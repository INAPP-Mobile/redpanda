{
  "KAFKA_BROKERS": {
    "value": "${{broker.RAILWAY_PRIVATE_DOMAIN}}:9092",
    "description": "Kafka bootstrap servers Console connects to. Defaults to the sibling broker service over the Railway private network (INTERNAL listener)."
  },
  "SCHEMA_REGISTRY_URL": {
    "value": "http://${{broker.RAILWAY_PRIVATE_DOMAIN}}:8081",
    "description": "Built-in Redpanda Schema Registry (enabled on the broker). Enables per-topic schema views in Console. Leave empty to disable."
  }
}
